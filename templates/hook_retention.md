# Hook Retention: {chapter}

status: 待评

official_chapter_sha256:
review_sha256:
accepted_at:
accepted_by:
reason:

## Contract Checked

- 对照 `Story Card` 检查第一屏扰动是否在正文前段落落地。
- 对照 `Story Card` 检查中段变化点是否真的改变主角行动条件。
- 对照 `Story Card` 检查本章小兑现和 `before -> after` 是否能在正文中找到证据。
- 对照 `Story Card` 检查章末点击理由是否不是单纯“待核状态”或空泛不合。
- 第 6 章起，连续三章同章末钩子类型会被 reader reward index / chapter shape 拦截。

## Findings

- 待评。

## Evidence Quotes

- 待填。

## Required Outcome

将 `status` 改为 `CLEAR`、`BLOCKED` 或 `ACCEPTED_BY_HUMAN`。若为 `ACCEPTED_BY_HUMAN`，必须填写 accepted_at、accepted_by、reason、official_chapter_sha256、review_sha256。
